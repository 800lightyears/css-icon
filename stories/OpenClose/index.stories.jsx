import React from 'react';
import OpenClose from './index'

export default {
  title: 'OpenClose',
  component: OpenClose,
  argTypes: {

  },
}

const Template = (args) => <OpenClose {...args} />

export const Example = Template.bind({});

Example.args = {

}

